MEMORY {
    ZP:   start = $0000, size = $0100, type = rw;
    ROM:  start = $FF00, size = $00FA, type = ro, fill = yes;
    ROMV: start = $FFFA, size = $0006, type = ro, fill = yes;
}

SEGMENTS {
    ZEROPAGE: load = ZP,   type = zp;
    STARTUP:  load = ROM,  type = ro;
    VECTORS:  load = ROMV, type = ro;
}
