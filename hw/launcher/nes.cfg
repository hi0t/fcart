MEMORY {
    ZP:  start = $0000, size = $0010;
    PRG: start = $FC00, size = $0400, fill = yes;
}

SEGMENTS {
    ZEROPAGE: load = ZP,  type = zp;
    CODE:     load = PRG, type = ro;
    VECTORS:  load = PRG, type = ro, start = $FFFA;
}
